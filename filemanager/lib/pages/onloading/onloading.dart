import 'package:flutter/material.dart';


class ONLOADING extends StatelessWidget{
  
  const ONLOADING ({super.key});
  
  @override

  Widget build(BuildContext context) {
      return Scaffold(
        body: Center(
          child: Text("ONLOADING"),
        ),
      );
  }
}